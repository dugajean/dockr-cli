services:
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: ${PROJECT_NAME}-pma
    links:
      - mysql
    expose:
      - 80
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      VIRTUAL_HOST: pma.${PROJECT_DOMAIN}
    volumes:
      - /sessions
